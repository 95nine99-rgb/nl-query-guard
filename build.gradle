plugins {
	id 'java'
	id 'org.springframework.boot' version '4.1.0'
	id 'io.spring.dependency-management' version '1.1.7'
}

group = 'dev.portfolio'
version = '0.0.1-SNAPSHOT'

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(21)
	}
}

repositories {
	mavenCentral()
}

dependencies {
	compileOnly 'org.projectlombok:lombok'
	annotationProcessor 'org.projectlombok:lombok'
	testCompileOnly 'org.projectlombok:lombok'
	testAnnotationProcessor 'org.projectlombok:lombok'
	implementation 'org.springframework.boot:spring-boot-starter-actuator'
	implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
	implementation 'org.springframework.boot:spring-boot-starter-validation'
	implementation 'org.springframework.boot:spring-boot-starter-webmvc'
	// SQL 을 AST 로 파싱한다.
	// 정규식은 문자열 리터잴·주석·대소문자를 구분하지 못해 7개 케이스 중 3개가 깨졌다 (ADR-002)
	implementation 'com.github.jsqlparser:jsqlparser:4.9'

	runtimeOnly 'com.mysql:mysql-connector-j'
	testImplementation 'org.springframework.boot:spring-boot-starter-actuator-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-data-jpa-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-validation-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-webmvc-test'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

tasks.named('test') {
	useJUnitPlatform {
		// CI 러너에는 Ollama 가 없다. 로컬에서만 돌린다.
		//   ./gradlew test -PincludeLocalLlm
		if (!project.hasProperty('includeLocalLlm')) {
			excludeTags 'local-llm'
		}
	}
	testLogging {
		// 기본값은 테스트 안의 println 을 숨긴다.
		// LLM 이 실제로 뭔가 뱉었는지 보려면 켜야 한다.
		showStandardStreams = true
		events 'passed', 'failed', 'skipped'
		exceptionFormat = 'full'
	}
}
