# 회고 — P1

> 게이트 질문에 대한 **최종 답변**을 여기 모은다.
> 면접에서 가장 많이 파고드는 문서다. 실패한 시도를 지우지 않는다.

## 게이트 질문과 답

### ① 왜 LLM 없이 먼저 만들었나? 규칙 기반 의도 분류는 어떤 질문에서 깨지나?
`8/27 작성`

### ② 내 화이트리스트를 우회하는 SQL을 하나 만들어봐라. 못 만들겠으면 왜 못 만드는지 설명해라.
`8/27 작성`

### ③ 신뢰도 임계값을 0.7로 정했다면 왜 0.9가 아닌가? 데이터로 답해라.
`8/30 작성`

### ④ 프롬프트를 바꿔 정확도가 떨어졌을 때, 그걸 사람이 아니라 무엇이 잡아주나?
`8/30 작성`

## 잘된 것

## 실패한 시도 (삭제 금지)

> 최소 1개. 여기가 비어 있으면 "한 번에 성공한 것"이 아니라 "기록을 안 한 것"으로 읽힌다.

## 다시 한다면

## 남은 과제
