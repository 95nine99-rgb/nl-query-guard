# P1 스케줄 — 2026-08-24(월) ~ 08-30(일) · **1주**

> 8/22 재조정으로 하루 밀림. 범위는 그대로. 하루 6~8시간 기준, 버퍼 없음.

## 8/24 (월) D1 — 준비 35분 + 기반
- [ ] `java -version` → 판단 ① 확정
- [ ] **branch protection 켜기 (판단 ②)** — 첫 커밋 전에 반드시
- [ ] git init x2 → GitHub push → CI 첫 실행 확인
- [ ] Context7 + GitHub MCP
- [ ] PRD 성공 기준 + ERD 확정 → **판단 ⑤(스키마 소유권)**
- [ ] 엔티티 3개: `Category` → `User` → `Transaction`
- [ ] Repository 3개, 시드 5천 건
- [ ] 조회 API `GET /api/transactions?categoryId=&from=&to=`

## 8/25 (화) D2 — 규칙 기반으로 끝까지
- [ ] 의도 5종 + 정답셋 30건
- [ ] 규칙 기반 의도 분류기
- [ ] 서버가 SQL 직접 조립 → E2E 연결
- [ ] → **LLM 없이 도는 데모**

## 8/26 (수) D3 — 먼저 깨뜨린다
- [ ] **방어 없이 실행 → 테이블 날려보고 스크린샷**
- [ ] 악의적 SQL 12종 작성
- [ ] 화이트리스트 파서 1차
- [ ] MySQL 읽기전용 MCP (`query_ro`)

## 8/27 (목) D4 — 다층 방어 · **게이트 1**
- [ ] 차단율 측정 (전/후)
- [ ] DENY 키워드 + 다중 구문 차단 + row limit + 타임아웃
- [ ] `user_id` 서버 강제 주입
- [ ] `infra/grant-readonly.sql` 실행 → `SHOW GRANTS` (users 없어야 정상)
- [ ] ADR-001, ADR-002

## 8/28 (금) D5 — LLM 투입 · **API 키 필요**
- [ ] 규칙 실패 질의 목록화 → LLM 도입 근거
- [ ] LLM 인터페이스 분리 + Mock 스텁
- [ ] 실제 LLM 연동 — 의도 분류만

## 8/29 (토) D6 — Text2SQL 완성
- [ ] WHERE 생성 LLM 위임 (SELECT 구조 서버 고정)
- [ ] 재생성 2회 + 신뢰도 임계값 + 되묻기
- [ ] 지수 백오프 + 타임아웃
- [ ] `prompts/` 파일 분리

## 8/30 (일) D7 — CI와 마무리 · **게이트 2**
- [ ] 정답셋 30건 회귀 스크립트
- [ ] **Actions 회귀 등록** — 기준선 미달 시 머지 차단
- [ ] Playwright MCP → 데모 GIF
- [ ] README · 07-ai-usage · 06-retro
- [ ] 태그 `v0.1-unguarded` / `v0.2-guarded`

---

## 게이트

### 목요일 (8/27)
> **①** 왜 LLM 없이 먼저 만들었나? 규칙 기반 의도 분류는 어떤 질문에서 깨지나?
> **②** 내 화이트리스트를 우회하는 SQL을 하나 만들어봐라. 못 만들겠으면 **왜** 못 만드는지 설명해라.

### 일요일 (8/30)
> **③** 신뢰도 임계값을 0.7로 정했다면 왜 0.9가 아닌가? 데이터로 답해라.
> **④** 프롬프트를 바꿔 정확도가 떨어졌을 때, 그걸 사람이 아니라 무엇이 잡아주나?

**게이트는 밀지 않는다.** 답 못 쓰면 진도 대신 하루를 더 쓴다. 답은 `docs/06-retro.md`에.

## 미결 판단

| | 내용 | 언제 |
|---|---|---|
| ① | JDK 배포판 | 8/24 아침 |
| ② | branch protection 순서 | 8/24 아침, 첫 커밋 전 |
| ④ | 테스트가 실제 DB를 필요로 하게 둘 것인가 | 8/24에 드러남, 늦어도 8/29까지 |
| ⑤ | 스키마 소유권 (`ddl-auto` vs 스크립트) | 8/24 ERD 확정 때 |

## 브랜치 규칙

| 브랜치 | 용도 | 수명 |
|---|---|---|
| `main` | 항상 동작하는 상태 | 영구 |
| `feat/*` | 기능 추가 | 2일 |
| `fix/*` | 버그 수정 | 1일 |
| `exp/*` | 측정 실험 — 결과가 나빠도 지우지 않고 태그만 | 2일 |
| `docs/*` `chore/*` | 문서·설정 | 1일 |

**main 직접 푸시 금지.** squash merge 기본.

## 커밋 컨벤션

`perf:`와 `exp:` 커밋에는 **반드시 수치를 적는다.**

```
exp: 정규식 검증은 주석 우회에 뚫림 — 12종 중 4종 통과
perf: 카테고리 합계 조회 p95 340ms → 90ms (5천건, 3회 중앙값)
```
